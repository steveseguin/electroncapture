name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v2.23.3)'
        required: true

permissions:
  contents: write

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22.12.0'
          cache: 'npm'

      - name: Validate release version
        shell: bash
        env:
          REQUESTED_VERSION: ${{ github.event_name == 'workflow_dispatch' && inputs.version || github.ref_name }}
        run: |
          package_version="$(node -p "require('./package.json').version")"
          requested_version="${REQUESTED_VERSION#v}"
          if [ "$requested_version" != "$package_version" ]; then
            echo "::error::Requested version $REQUESTED_VERSION does not match package.json version v$package_version"
            exit 1
          fi
          echo "Validated release version v$package_version"

      - name: Run regression tests
        run: npm test

      - name: Audit dependency lockfiles
        run: npm run audit:dependencies

  build-windows:
    needs: validate
    # node-gyp 10 does not recognize the Visual Studio 2026 image currently
    # selected by windows-latest. Keep the native ASIO build on VS 2022.
    runs-on: windows-2022
    steps:
      - name: Require private submodule key
        shell: pwsh
        env:
          SUBMODULE_SSH_KEY: ${{ secrets.SUBMODULE_SSH_KEY }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:SUBMODULE_SSH_KEY)) {
            throw 'SUBMODULE_SSH_KEY is required for complete Windows x64 builds.'
          }

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive
          ssh-key: ${{ secrets.SUBMODULE_SSH_KEY }}
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22.12.0'
          cache: 'npm'

      - name: Restore Windows signing certificate
        shell: pwsh
        env:
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}
          WIN_CSC_PFX_BASE64: ${{ secrets.WIN_CSC_PFX_BASE64 }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:WIN_CSC_PFX_BASE64)) {
            throw 'WIN_CSC_PFX_BASE64 is required for signed Windows builds.'
          }
          if ([string]::IsNullOrWhiteSpace($env:WIN_CSC_KEY_PASSWORD)) {
            throw 'WIN_CSC_KEY_PASSWORD is required for signed Windows builds.'
          }
          New-Item -ItemType Directory -Force -Path certs | Out-Null
          [System.IO.File]::WriteAllBytes(
            'certs/electroncapture.pfx',
            [System.Convert]::FromBase64String($env:WIN_CSC_PFX_BASE64)
          )

      - name: Install dependencies
        run: npm ci

      - name: Audit installed dependencies
        run: |
          npm --prefix native-modules/window-audio-capture audit --package-lock-only --audit-level=low
          npm run audit:signatures
          npm --prefix native-modules/window-audio-capture audit signatures

      - name: Build Windows x64
        run: npm run build:win32
        env:
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}

      - name: Verify Windows x64 package
        run: npm run verify:win32

      - name: Verify Windows x64 signatures
        shell: powershell
        timeout-minutes: 5
        run: .\scripts\verify-windows-signatures.ps1 -Architecture x64

      - name: Build Windows 11 x64
        run: npm run build:win11
        env:
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}

      - name: Verify Windows 11 x64 package
        run: npm run verify:win11

      - name: Verify Windows 11 x64 signatures
        shell: powershell
        timeout-minutes: 5
        run: .\scripts\verify-windows-signatures.ps1 -Architecture x64 -Variant win11

      - name: Test Windows installer replacement and rollback
        shell: powershell
        run: |
          $version = (Get-Content -Raw .\package.json | ConvertFrom-Json).version
          .\scripts\test-windows-installer-roundtrip.ps1 `
            -FallbackInstaller ".\dist\elecap-$version.exe" `
            -CurrentInstaller ".\dist\elecap-$version-win11.exe"

      - name: Submit executables to VirusTotal
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        continue-on-error: true
        run: node scripts/submit-virustotal.js --dir dist --require-api-key --fail-on-error
        env:
          VIRUSTOTAL_API_KEY: ${{ secrets.VIRUSTOTAL_API_KEY }}

      - name: Upload to Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          fail_on_unmatched_files: true
          files: |
            dist/elecap_win_*_portable.zip
            dist/elecap_win_*_installer.zip
            dist/elecap_win11_*_portable.zip
            dist/elecap_win11_*_installer.zip
            dist/elecap-*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (for workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: windows-builds
          if-no-files-found: error
          retention-days: 14
          path: |
            dist/elecap_win_*_portable.zip
            dist/elecap_win_*_installer.zip
            dist/elecap_win11_*_portable.zip
            dist/elecap_win11_*_installer.zip
            dist/elecap-*.exe

  build-windows-arm64:
    needs: validate
    runs-on: windows-11-arm
    env:
      npm_config_arch: arm64
      ELECTRON_INSTALL_ARCH: arm64
      ELECTRON_ASIO_SKIP: '1'
      WINDOW_AUDIO_CAPTURE_SKIP: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup ARM64 Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22.12.0'
          architecture: arm64
          cache: 'npm'

      - name: Restore Windows signing certificate
        shell: pwsh
        env:
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}
          WIN_CSC_PFX_BASE64: ${{ secrets.WIN_CSC_PFX_BASE64 }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:WIN_CSC_PFX_BASE64)) {
            throw 'WIN_CSC_PFX_BASE64 is required for signed Windows builds.'
          }
          if ([string]::IsNullOrWhiteSpace($env:WIN_CSC_KEY_PASSWORD)) {
            throw 'WIN_CSC_KEY_PASSWORD is required for signed Windows builds.'
          }
          New-Item -ItemType Directory -Force -Path certs | Out-Null
          [System.IO.File]::WriteAllBytes(
            'certs/electroncapture.pfx',
            [System.Convert]::FromBase64String($env:WIN_CSC_PFX_BASE64)
          )

      - name: Install dependencies
        run: npm ci

      - name: Build Windows ARM64
        run: npm run build:win-arm64
        env:
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}

      - name: Verify Windows ARM64 package
        run: npm run verify:win-arm64

      - name: Verify Windows ARM64 signatures
        shell: powershell
        timeout-minutes: 5
        run: .\scripts\verify-windows-signatures.ps1 -Architecture arm64

      - name: Submit executables to VirusTotal
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        continue-on-error: true
        run: node scripts/submit-virustotal.js --dir dist --require-api-key --fail-on-error
        env:
          VIRUSTOTAL_API_KEY: ${{ secrets.VIRUSTOTAL_API_KEY }}

      - name: Upload to Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          fail_on_unmatched_files: true
          files: |
            dist/*win-arm64*.exe
            dist/*_arm64_*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (for workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: windows-arm64-builds
          if-no-files-found: error
          retention-days: 14
          path: |
            dist/*win-arm64*.exe
            dist/*_arm64_*.zip

  build-macos:
    needs: validate
    runs-on: macos-15
    env:
      CUSTOM_ELECTRON_SKIP: '1'
      ELECTRON_ASIO_SKIP: '1'
      WINDOW_AUDIO_CAPTURE_SKIP: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Validate macOS signing secrets
        shell: bash
        env:
          CSC_LINK: ${{ secrets.MAC_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.MAC_CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
        run: |
          missing=()
          [ -n "$CSC_LINK" ] || missing+=("MAC_CSC_LINK")
          [ -n "$CSC_KEY_PASSWORD" ] || missing+=("MAC_CSC_KEY_PASSWORD")
          [ -n "$APPLE_ID" ] || missing+=("APPLE_ID")
          [ -n "$APPLE_TEAM_ID" ] || missing+=("APPLE_TEAM_ID")
          [ -n "$APPLE_APP_SPECIFIC_PASSWORD" ] || missing+=("APPLE_APP_SPECIFIC_PASSWORD")
          if [ ${#missing[@]} -ne 0 ]; then
            echo "::error::Missing required macOS signing secrets: ${missing[*]}"
            exit 1
          fi

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22.12.0'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build, sign, and notarize macOS universal package
        run: npm run build:darwin -- --config.mac.forceCodeSigning=true
        env:
          CSC_LINK: ${{ secrets.MAC_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.MAC_CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}

      - name: Verify macOS package
        run: npm run verify:darwin
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Upload to Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          fail_on_unmatched_files: true
          files: |
            dist/*.dmg
            dist/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (for workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: macos-universal-builds
          if-no-files-found: error
          retention-days: 14
          path: |
            dist/*.dmg
            dist/*.zip

  build-linux:
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22.12.0'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci
        env:
          WINDOW_AUDIO_CAPTURE_SKIP: '1'

      - name: Build Linux
        run: npm run build:linux

      - name: Upload to Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          fail_on_unmatched_files: true
          files: |
            dist/*.AppImage
            dist/*.deb
            dist/*.rpm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (for workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linux-builds
          if-no-files-found: error
          retention-days: 14
          path: |
            dist/*.AppImage
            dist/*.deb
            dist/*.rpm

  build-linux-arm64:
    needs: validate
    runs-on: ubuntu-24.04-arm
    env:
      npm_config_arch: arm64
      ELECTRON_INSTALL_ARCH: arm64
      WINDOW_AUDIO_CAPTURE_SKIP: '1'
      ELECTRON_ASIO_SKIP: '1'
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup ARM64 Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22.12.0'
          architecture: arm64
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Linux ARM64
        run: npm run build:arm64

      - name: Verify Linux ARM64 package
        run: npm run verify:linux-arm64

      - name: Upload to Release
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          fail_on_unmatched_files: true
          files: |
            dist/*-arm64.AppImage
            dist/*-arm64.deb
            dist/*-aarch64.rpm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts (for workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linux-arm64-builds
          if-no-files-found: error
          retention-days: 14
          path: |
            dist/*-arm64.AppImage
            dist/*-arm64.deb
            dist/*-aarch64.rpm
